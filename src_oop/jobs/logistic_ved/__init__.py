"""Пакет процессов логистики ВЭД.

Бизнес-логика пакета:
- закупщики ведут первичные данные в таблице ``Заказы белые ТЕСТ``;
- логисты ведут операционные данные в таблице ``ОТЧЁТ_2.0``;
- прямая синхронизация переносит в работу логистов только отобранные строки;
- обратная синхронизация возвращает закупщикам логистические статусы и даты;
- оба потока работают по ключу ``ORDER_LINE_ID``.

Техническая логика пакета:
- модуль ``run.py`` отвечает за прямую синхронизацию в ``ОТЧЁТ_2.0``;
- модуль ``reverse_run.py`` отвечает за обратную синхронизацию в ``Заказы белые ТЕСТ``;
- модуль ``config.py`` хранит набор колонок и названия рабочих листов;
- пакет экспортирует только боевую точку входа полного сценария.
"""

from src_oop.jobs.logistic_ved.run import logistic_ved_full_run

__all__ = ["logistic_ved_full_run"]
